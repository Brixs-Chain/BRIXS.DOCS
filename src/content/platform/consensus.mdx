# Consensus: Decentralized Sequencer & optimized execution engine

The core engine securing the Brixs Network is the **Proof of Authority (PoA) Sequencer** combined with our highly advanced **optimized execution engine Engine**.

<Callout type="success" title="Testnet Environment">
The Sequencer is currently running in Testnet. During this phase, validators are tested, and ZK proofs are verified locally before being deployed to the Ethereum L1 Mainnet.
</Callout>

## Key Characteristics

<FeatureGrid>
  <FeatureCard 
    icon="Network" 
    title="Proof of Authority (PoA) Sequencer" 
    description="A staking manager allowing anyone to stake BRIXS to propose blocks, eliminating centralized sequencer failure points."
  />
  <FeatureCard 
    icon="ShieldAlert" 
    title="Sequencer Mempool" 
    description="Protects users against MEV (Miner Extractable Value) sandwich attacks by encrypting transactions before they are ordered."
  />
  <FeatureCard 
    icon="Cpu" 
    title="optimized execution engine" 
    description="leverages an optimized execution engine, setting the foundation for future scalable architectures."
  />
  <FeatureCard 
    icon="Workflow" 
    title="EVM Rollup" 
    description="Transactions are processed natively via @ethereumjs/vm and batched sequentially into secure rollups."
  />
</FeatureGrid>

## Deep Dive: How the Sequencer Works

### Phase 1: Transaction Encryption
When a user submits a transaction (e.g. via our Account Abstraction Paymaster), it enters the **Sequencer Mempool**. This guarantees that block proposers cannot front-run or sandwich user trades.

### Phase 2: DPoS Block Proposal
Validators who have staked `$BRIXS` are selected dynamically to propose the next block. This removes the "single sequencer" risk present in older L2s like Optimism and Arbitrum. The node processes the transactions natively using our optimized `@ethereumjs/vm` gateway.

### Phase 3: ZK Proof Generation
Once the block is assembled and state transitions are finalized locally, our Prover Engine generates a zero-knowledge cryptographic proof of the execution. This proof guarantees mathematically that all state changes were valid.

```javascript
// Connect to the Testnet RPC to observe blocks
const provider = new ethers.providers.JsonRpcProvider("https://rpc-testnet.brixs.space");
const block = await provider.getBlock("latest");
console.log(`Current Sequencer Block: ${block.number}`);
```
