# Consensus: Brixs Sequencer

The core engine securing the Brixs Network is **Brixs Sequencer**, a highly optimized Directed Acyclic Graph (DAG) consensus protocol.

<Callout type="warning" title="Architectural Change">
Brixs Sequencer completely removes the concept of sequential block mining. Transactions are ordered deterministically based on their cryptographic graph references, drastically reducing latency.
</Callout>

## Key Characteristics

<FeatureGrid>
  <FeatureCard 
    icon="Network" 
    title="Leaderless Architecture" 
    description="No single node dictates block creation, eliminating bottlenecks and MEV extraction vectors."
  />
  <FeatureCard 
    icon="Timer" 
    title="Asynchronous Processing" 
    description="Nodes propose and validate vertices continuously without waiting for discrete block times."
  />
  <FeatureCard 
    icon="ShieldAlert" 
    title="Byzantine Fault Tolerant" 
    description="Can sustain 1/3 malicious nodes without halting or forking the network."
  />
  <FeatureCard 
    icon="Workflow" 
    title="DAG Structure" 
    description="Transactions reference previous transactions, forming an interwoven, immutable graph."
  />
</FeatureGrid>

## Deep Dive: How Brixs Sequencer Handles Faults


### Phase 1: Engine Initialization
The first phase requires a comprehensive setup of the execution environment. <Keyword>BrixsVM</Keyword> allocates memory blocks and establishes secure P2P connections via the gossip protocol. This ensures that when <Keyword>Brixs Sequencer</Keyword> begins proposing blocks, there are no I/O bottlenecks. 

```bash
# Initialize the Brixs node
brixs deploy
```

### Phase 2: State Merklization
Raw blockchain data is ingested into the <Keyword>Brixs Trie</Keyword>, our lock-free, highly concurrent sparse Merkle tree built over RocksDB. Hot state is cached in RAM to guarantee microsecond retrieval times. 

```javascript
// Connect to the high-throughput RPC
const provider = new BrixsProvider("http://localhost:8545");
const block = await provider.getLatestBlock();
console.log(`Current Block: ${block.number}`);
```

### Phase 3: Consensus & Finality
Transactions are mapped into a DAG. <Keyword>BrixsVM</Keyword> dispatches non-conflicting transactions to the thread pool for parallel execution. Once processed, <Keyword>Brixs Sequencer</Keyword> pipelines the block proposal, achieving Instant Rollup Finality in under 400ms.

