# Metrics & Monitoring

Brixs nodes expose a native Prometheus metrics endpoint on port `9100`. Robust monitoring is required for production deployments.

## Key Metrics to Monitor

<FeatureGrid>
  <FeatureCard 
    icon="Activity" 
    title="P2P Peers" 
    description="Monitor `brixs_network_peers` to ensure you remain connected to the gossip network."
  />
  <FeatureCard 
    icon="Clock" 
    title="Epoch Lag" 
    description="Monitor `brixs_consensus_epoch` to ensure your node is perfectly synced."
  />
  <FeatureCard 
    icon="Database" 
    title="DB Size" 
    description="Monitor `brixs_db_size_bytes` to plan for storage volume expansions."
  />
</FeatureGrid>

## Alert Triage Volume

<AnimatedPieChart 
  dataString='[
    { "name": "P2P Sync Issues", "value": 50 },
    { "name": "Storage Alerts", "value": 30 },
    { "name": "CPU Bottlenecks", "value": 15 },
    { "name": "OOM Kills", "value": 5 }
  ]' 
/>

## Setting up Alerts


### Phase 1: Engine Initialization
The first phase requires a comprehensive setup of the execution environment. The <Keyword>Brixs Native EVM</Keyword> initializes the state and establishes secure connections with the P2P network. This ensures that when the <Keyword>DPoS Sequencer</Keyword> begins proposing blocks, there are no bottlenecks in transaction ingestion. 

Transactions are ordered securely by the Encrypted Mempool to prevent MEV. The <Keyword>Native EVM</Keyword> processes transactions while the <Keyword>ZK-SNARK Prover Engine</Keyword> generates validity proofs. Once processed, the <Keyword>Sequencer</Keyword> commits the block to the Ethereum L1, achieving Rollup Finality and inheriting Ethereum\'s cryptographic security.

